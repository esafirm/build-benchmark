// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Most plugins is added to classpath in buildSrc
    alias(libs.plugins.kotlin.compose) apply false
}
